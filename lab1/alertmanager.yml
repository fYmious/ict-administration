route:
  receiver: "default-webhook"
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 1h

receivers:
  - name: "default-webhook"
    webhook_configs:
      - url: "http://webhook-receiver:5001/alert"
        send_resolved: true
